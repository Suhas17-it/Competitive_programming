#include <bits/stdc++.h>
int main(){

    vector<vector<char>> mat(8,vector<char>(8));

    for(int i = 0 ; i < 8 ; i ++){
        for(int j = 0 ; j < 8 ; j ++){
            cin >> mat[i][j];
        }
    }

    
}